# Everything here is development-only and must never reach the plugin zip.
/.git
/.github
/.wordpress-org
/bin
/.gitignore
/.distignore
/README.md
/node_modules
/tests
# translate.wordpress.org supplies translations for the hosted plugin
/languages
